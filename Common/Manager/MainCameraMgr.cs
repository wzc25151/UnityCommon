using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainCameraMgr : SingeBase<MainCameraMgr>
{

    // void LateUpdate()
    // {
    //     Transform playerTrans = PlayerContorler.instance.transform;
    //     Vector3 temp = new Vector3(playerTrans.position.x, playerTrans.position.y, transform.position.z);
    //     transform.position = Vector3.Lerp(transform.position, temp, 3 * Time.deltaTime);
    // }
}
